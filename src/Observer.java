/* It's just a learning example
   in a real project there are will be
   Generics and Events for better
   architecture
 */
public interface Observer {
    public void update(float temperature, float humidity, float pressure);
}
